DECISION: PASS
SUMMARY: P2 auditor PASS. ReferencePopoverLink + ReferencePopoverBody are correctly composed around the existing Radix Tooltip primitives (no new dependency). Native title attribute is removed from all reference anchors, while data-reference/data-reference-status/data-reference-kind attributes are preserved for testing/automation. Popover content covers resolved, missing, ambiguous, blocked, and local-file states with stable data-reference-popover-* selectors. Blocked .plan/_private paths and outside-root files are routed through the popover (with markdownUrlTransform preserving the original href) rather than the old #blocked-link fallback. Ordinary local/external/mailto links keep their safe href behavior without any popover/title noise. Keyboard accessibility relies on Radix TooltipTrigger asChild + sr-only source label, and portal/clipping is handled by TooltipPrimitive.Portal. Privacy regressions still pass (3 tests) and Markdown viewer tests now cover resolved, missing, blocked, ordinary, and title-removal cases.
REQUIRED_CORRECTIONS:
- None
