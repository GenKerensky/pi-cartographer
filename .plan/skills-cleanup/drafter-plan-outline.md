Read-only draft only. I did **not** write `/var/home/falco/code/pi-cartographer/skills-cleanup/.plan/skills-cleanup/drafter-plan-outline.md` because the task also says “Read-only only; do not mutate files” and the acceptance criterion requires “no file mutations.”

## Suggested Plan Outline

| Phase | Name | Depends on | Objective | Key tasks | Validation commands |
|---|---|---:|---|---|---|
| P0 | Inventory and lint baseline | — | Inventory stale skill language without changing workflow/userflow. | Review target `SKILL.md` files; classify direct JSONL-write wording, stale delegation prompts, RFC 2119 misuse, line counts, and script gaps. Treat currently uncommitted `AGENTS.md` guardrails as baseline. | `git status --short`; `rg -n "(hand-edit|append|upsert|create .*jsonl|update .*jsonl|plan\\.nodes|plan\\.edges|interview\\.nodes|facts\\.nodes|map\\.nodes)" skills/{proposal,plan,interview,implement,index-project,dashboard}/SKILL.md AGENTS.md` |
| P1 | Script-first deterministic skill checks | P0 | Convert repeatable language checks into deterministic lint. | Add/refine a bounded skill-language checker for direct JSONL writes, wrapper-first wording, ADR-0004 delegation terms, RFC 2119 usage, frontmatter trigger clarity, and line-count/reference limits. | `python skills/plan/scripts/check_skill_language.py --root "$PWD" --json`; `npm run check:scripts` |
| P2 | Wrapper-first skill rewrites | P1 | Rewrite target skills to route mutations through wrappers and preserve workflow shape. | Update proposal/plan/interview/implement/index/dashboard skill prose so JSONL is readable context but writes go through `cartographer_*` wrappers or narrow scripts; remove legacy “create/update/upsert JSONL” wording; keep proposal → optional interview/requirements/design → plan → implement flow unchanged. | `python skills/plan/scripts/check_skill_language.py --root "$PWD" --json`; `rg -n "(append|upsert|hand-edit|prose-only|create .*jsonl|update .*jsonl)" skills/{proposal,plan,interview,implement,index-project,dashboard}/SKILL.md` |
| P3 | ADR-0004 delegation review | P2 | Ensure delegation language matches single-writer/read-only specialist boundaries. | Reframe prompts so parent/current agent owns canonical writes; specialists return read-only findings/drafts; pathfinder remains explicit legacy opt-in only; remove generic planner/researcher/scout write authority. | `rg -n "(subagent|specialist|planner|researcher|scout|pathfinder|write|mutate|append|upsert)" skills/{proposal,plan,interview,implement,index-project,dashboard}/SKILL.md .pi/agents/cartographer-*.md` |
| P4 | Narrow wrapper/script gap resolution | P2, P3 | Avoid direct JSONL-write prose where wrappers are missing. | For interview or other remaining write gaps, either add a narrow wrapper/script or document an explicit temporary fallback boundary with validation evidence; stop if this would change durable workflow behavior. | `npm run check:scripts`; `npm run typecheck`; targeted tests for any changed wrapper/script |
| P5 | Validation and parent-owned audit evidence | P1–P4 | Prove cleanup is deterministic, scoped, and ADR-aligned. | Run skill-language lint, package checks, targeted grep checks, and prepare evidence for parent-owned audit/receipt capture. Do not change lifecycle gates or userflow. | `npm run check`; `node --experimental-strip-types skills/plan/scripts/manage_jsonl.ts validate-topic --root "$PWD" --topic skills-cleanup --json`; `python skills/plan/scripts/validate_planning_graph.py --root "$PWD" --topic skills-cleanup --json` |

References used: proposal scope/next artifacts; facts F001–F012; ADR-0004; ADR-0006; context pack `context-pack:skills-cleanup:proposal`; map refs for target skill files and wrappers.